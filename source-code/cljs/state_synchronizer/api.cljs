
(ns state-synchronizer.api
    (:require [state-synchronizer.views :as views]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (state-synchronizer.views/*)
(def sensor views/sensor)
 
